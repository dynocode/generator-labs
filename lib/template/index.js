const template = require('./create-files');
const test = require('./test-template');
const { defaultFilePaths } = require('./file-paths');

module.exports = {
  defaultFilePaths,
  template,
  test,
};
